// import { SDK } from "../src";
// import { BlockchainType } from "../src/types";
// import * as eos from "../src/blockchains/eos";
import * as ethereum from "../src/blockchains/ethereum";
import * as ethereumMocks from "./mocks/ethereum";

describe("get converter's reserves and weight", () => {
  // beforeEach(async () => {
  //   jest
  //     .spyOn(ethereum, "getWeb3")
  //     .mockImplementationOnce(ethereumMocks.getWeb3);
  // });

  afterEach(async () => {});

  it("gets etheruem values from given array of converter addresses", () => {});
});
